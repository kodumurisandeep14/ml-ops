GitHub
    Repository 

Jenkins
    pipeline

Docker
    image (Run in all OS)

EC2 Virtual Server. (AWS)

1. Create EC2 Instance

2. enable below ports
    a) 8000 uvicorn TCP
    b) 8080 Jenkins TCP
    c) 80 Docker HTTP
3. Update Ubuntu
    sudo apt update -y
4. Install Jenkins
    Jenkins.io
5. Install Docker
    sudo apt update 
    sudo apt install docker.io -y
    docker --version 
6. enable permission globally 
    sudo usermod -aG docker jenkins

7. get jenkins password
    sudo cat/var/lib/jenkins/secrets/initialAdminPassword
    5afa974531194c839b0f3a3d051a45dc
8. Create a Dockerfile
    copy code to Docker image to build docker image  
9. Push application to GitHub

10.create pipeline
    Jenkinsfile
11. Push Jenkinsfile to GitHub  
        Git add .
        Git commit -m "jenkins file added"
        Git push -u origin main
12. connect to jenkins with EC2 IP Address
        http://18.191.30.192:8080
            password
        PlugIns
            1.Git
            2.Docker
            3.Docker pipeline
            4.gitserver
13. create a pipleline
